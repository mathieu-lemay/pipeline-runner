- Proper docker cache (save / restore images)
- Use static docker binary (only if docker service)
- Figure out git binary
- Support for pipeline specific variables
- Read pipeline variables from some file (with support for hidden)
- Retry pipeline from step
- Clone the real thing (with depth config)
- pipes
- Execution report
- Isolated network
- SSH Keys
- --include / --exclude instead of --step

- Read pipeline variables from some file (with support for hidden)
- Retry pipeline from step
- Clone the real thing
- pipes
- Execution report
- Isolated network
- SSH Keys
- --include / --exclude instead of --step
- Validate there are no variables in branch / pull-request pipelines
- Validate only one "variables:" block and it's defined first
- Use docker volume for docker cache
